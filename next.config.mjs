/** @type {import('next').NextConfig} */
const nextConfig = {
  /* -----------------------------------------------------------------------
   * Next.js configuration for TAVAS Labs website
   * Add image domains here if you later use next/image with external URLs
   * ----------------------------------------------------------------------- */
};

export default nextConfig;
